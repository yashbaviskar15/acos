[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["arvgate/tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.format]
quote-style = "double"
