# Docker

Local development orchestration lives here. `docker-compose.yml` wires together Postgres, Redis, every FastAPI service, the React frontend, the Nginx gateway, and the observability stack so the platform can be exercised end to end on one machine.
